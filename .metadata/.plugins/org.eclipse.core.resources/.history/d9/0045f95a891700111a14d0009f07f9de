package com.dcl;

import java.time.LocalDate;
import java.util.Arrays;
import java.util.Optional;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.ConfigurableApplicationContext;

import com.dcl.entity.Book;
import com.dcl.repo.BookRepo;

@SpringBootApplication
public class Application {

	public static void main(String[] args) {
		ConfigurableApplicationContext context=SpringApplication.run(Application.class, args);
		 BookRepo repository=context.getBean(BookRepo.class);
		
		 Book b1=new Book();
		 b1.setBookId(2);
		 b1.setBookName("Spring");
		 b1.setAuthor("Rod Johnson");
		 b1.setPrice(1000.0);
		 b1.setReleaseDate(LocalDate.now());
		 repository.save(b1);
		
		//b1=repository.save(b1); //---1
		//System.out.println("Book saved successfully!");
		//System.out.println(b1);
		
		//Book b2=new Book(2,"Ramayana-Darshanam","Kuvempu",1000.0,LocalDate.now());
		//Book b3=new Book(3,"Mahabharat","Vyasa",2000.0,LocalDate.now());
		//repository.save(b1); --1
		//repository.saveAll(Arrays.asList(b2,b3)); --2
		//repository.findById(4).orElse(null); --3
		//repository.findAll().forEach(System.out::println); //--4
		//repository.findAllById(Arrays.asList(1,2)).forEach(System.out::println); -- 5
		//repository.existsById(1);  -- 6
		//repository.count(); -- 7
		//repository.deleteAllById(Arrays.asList(2,3)); --8
		//repository.deleteAll(); --9
		//repository.deleteAll(Arrays.asList(b1,b2,b3)); --10
		//repository.delete(b1); --11
		//repository.deleteById(1); --12
		 
		 repository.findByAuthor("Rod Johnson").forEach(System.out::println);
		 repository.findByPriceGreaterThanEqual(500.0).forEach(System.out::println);
		 repository.findByBookNameStartsWith('S');
	}

}
